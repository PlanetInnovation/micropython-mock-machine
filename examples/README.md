# examples

Here are some examples that use `mock_machine`.

test_tmp117.py unit tests the tmp117 driver using a mock I2C class from `mock_machine`.

test_hardware_revision.py unit tests the harware_revision driver using a mock SPI and mock PIN class
from `mock_machine`.
